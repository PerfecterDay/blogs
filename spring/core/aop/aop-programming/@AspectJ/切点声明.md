# 声明切点
{docsify-updated}

> https://docs.spring.io/spring-framework/reference/core/aop/ataspectj/pointcuts.html


切点决定了我们关注的连接点，从而使我们能够控制增强逻辑何时运行。Spring AOP仅支持Spring Bean的方法连接点，因此可以将切点理解为**匹配Spring Bean上方法的执行**。切点的声明包含两部分：
+ 由名称及参数组成的签名 - the pointcut signature
+ 以及用于精确匹配目标方法执行的切点表达式。- the pointcut expression

在采用 `@AspectJ` 注解风格的AOP中，切点签名通过**常规方法**定义实现，而切点表达式则通过 `@Pointcut` 注解标识（作为切点签名的方法必须具有 `void` 返回类型）。

```
@Pointcut("execution(* transfer(..))") // the pointcut expression
private void anyOldTransfer() {} // the pointcut signature
```
构成 `@Pointcut` 注解值的切点表达式是标准的 `AspectJ` 切点表达式。


## 支持的切点标识符
Spring AOP 支持在切点表达式中使用以下 `AspectJ` 切点标识符 (PCD)：
+ `execution` ：For matching method execution join points. This is the primary pointcut designator to use when working with Spring AOP.
+ `within` ：Limits matching to join points within certain types (the execution of a method declared within a matching type when using Spring AOP).
+ `this` ： Limits matching to join points (the execution of methods when using Spring AOP) where the bean reference (Spring AOP proxy) is an instance of the given type. （AOP生成的代理对象）
+ `target` : Limits matching to join points (the execution of methods when using Spring AOP) where the target object (application object being proxied) is an instance of the given type. （被代理的目标对象）
+ `args` : Limits matching to join points (the execution of methods when using Spring AOP) where the arguments are instances of the given types.
+ `@target` ：Limits matching to join points (the execution of methods when using Spring AOP) where the class of the executing object has an annotation of the given type.
+ `@args` ：Limits matching to join points (the execution of methods when using Spring AOP) where the runtime type of the actual arguments passed have annotations of the given types.
+ `@within` ：Limits matching to join points within types that have the given annotation (the execution of methods declared in types with the given annotation when using Spring AOP).
+ `@annotation` ：Limits matching to join points where the subject of the join point (the method being run in Spring AOP) has the given annotation.
+ `bean` : This PCD lets you limit the matching of join points to a particular named Spring bean or to a set of named Spring beans (when using wildcards). The bean PCD has the following form: `bean(idOrNameOfBean)`

完整的 `AspectJ` 切入点语言支持Spring未支持的额外切点标识符： `call` 、 `get` 、 `set` 、 `preinitialization` 、 `staticinitialization` 、 `initialization` 、 `handler` 、 `adviceexecution` 、 `withincode` 、 `cflow` 、 `cflowbelow` 、 `if` 、 `@this` 和 `@withincode` 。在Spring AOP解释的切入点表达式中使用这些标识符将导致抛出 `IllegalArgumentException` 异常。

Spring AOP支持的切入点标识符集在未来版本中可能会扩展，以支持更多 `AspectJ` 切点标识符。

此外，AspectJ本身具有基于类型的语义，在执行连接点处，this和target都指向同一个对象：即正在执行该方法的对象。Spring AOP作为基于代理的系统，会区分代理对象本身（绑定至this）与代理背后的目标对象（绑定至target）。

由于Spring的AOP框架基于代理的特性，目标对象内部的调用不会被拦截。对于JDK代理，仅能拦截代理上的公共接口方法调用。而使用 `CGLIB` 时，代理上的公共方法和受保护方法调用均可被拦截（必要时甚至包括 package-visible 的方法）。不过，通常代理方法通常都声明为 `public` 。

## 组合切点表达式
可以使用 `&&` 、 `||` 和 `!` 运算符组合点切表达式。也可以通过名称引用点切表达式。以下示例展示了三个点切表达式：
```
public class Pointcuts {

	@Pointcut("execution(public * *(..))")
	public void publicMethod() {}

	@Pointcut("within(com.xyz.trading..*)")
	public void inTrading() {}

	@Pointcut("publicMethod() && inTrading()")
	public void tradingOperation() {}
}
```
1. `publicMethod`: 匹配条件为：方法执行连接点代表任何公共方法的执行。
2. `inTrading`: 匹配 trading module 下的任何方法
3. `tradingOperation` : 匹配 trading module 下的任何公共方法

如上所述，**将较小的命名切入点组合成更复杂的切入点表达式是一种最佳实践**。通过名称引用切入点时，遵循常规的Java可见性规则（例如：同一类型内的 `private` 切入点可见，继承层次中的 `protect` 切入点可见， `public` 切入点在任何位置可见，等等）。可见性不影响切入点匹配。

## 共享命名切点定义-Sharing Named Pointcut Definitions

## Examples
Spring AOP用户最常使用的是 `execution` 切点表达式。 `execution` 表达式的格式如下：
```
execution(modifiers-pattern?
			ret-type-pattern
			declaring-type-pattern?name-pattern(param-pattern)
			throws-pattern?)
```

除了 `ret-type-pattern` 、 `name-pattern` 、 `param-pattern` 外，其它都是可选的：
+ `ret-type-pattern`：决定了方法必须返回何种类型才能匹配接入点 。 `*` 是最常用的返回类型模式，可匹配任意返回类型。如果是完全限定类型名，则仅在方法返回指定类型时才能匹配。
+ `name-pattern` ： 用于匹配方法名称。 可全部或部分使用 * 通配符。若指定了 `declaring-type-pattern` ，需添加尾随 `.` 符号将其与名称模式组件连接。
+ `param-pattern` : 参数模式稍显复杂。
  + `()` 匹配无参数的方法； 
  + `(..)` 匹配任意数量（零个或多个）参数; 
  + `(*)` 匹配接受单个任意类型参数的方法。
  + `(*,String)` 匹配接受两个参数的方法：第一个参数类型不限，第二个必须为字符串类型。

```
execution(public * *(..)) // 任意公共方法
execution(* set*(..)) //任意的 setXXX 方法
execution(* com.xyz.service.AccountService.*(..)) //com.xyz.service.AccountService 中的任意方法
execution(* com.xyz.service.*.*(..)) //com.xyz.service 包下的任意方法
execution(* com.xyz.service..*.*(..)) // com.xyz.service 及其子包下的任意方法
within(com.xyz.service.*) //com.xyz.service 下的任意方法
within(com.xyz.service..*) // com.xyz.service 及其子包下的任意方法
this(com.xyz.service.AccountService) //
target(com.xyz.service.AccountService) // 任意实现了 AccountService 的目标对象

args(java.io.Serializable) //任何运行时参数为Serializable非方法 请注意，本例中给出的切入点与 execution(* *(java.io.Serializable)) 不同。args 版本匹配条件是运行时传递的参数为Serializable类型，而 execution 版本匹配条件是方法签名声明了单个Serializable类型的参数。

@target(org.springframework.transaction.annotation.Transactional) //Any join point (method execution only in Spring AOP) where the target object has a @Transactional annotation:

@within(org.springframework.transaction.annotation.Transactional) //Any join point (method execution only in Spring AOP) where the declared type of the target object has an @Transactional annotation:

@annotation(org.springframework.transaction.annotation.Transactional) //Any join point (method execution only in Spring AOP) where the executing method has an @Transactional annotation:

@args(com.xyz.security.Classified) // Any join point (method execution only in Spring AOP) which takes a single parameter, and where the runtime type of the argument passed has the @Classified annotation:

bean(tradeService) //Any join point (method execution only in Spring AOP) on a Spring bean named tradeService:

bean(*Service) //Any join point (method execution only in Spring AOP) on Spring beans having names that match the wildcard expression *Service
```

## 编写好的切点
